[package]
name = "misc"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-m = { workspace = true }
aes-gcm = { version = "0.10.2", default-features = false, features = ["aes", "alloc"] }
aes = "0.8.2"

[features]
default = []